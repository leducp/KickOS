# SPDX-License-Identifier: CECILL-C
# Copyright (c) 2026 Philippe Leduc
#
# i.MX 8M Plus EVK. No pads and no driver handover yet, so the board declares neither a
# service list nor a pin map; what IS this board's rather than its chip's is how much RAM it
# has, which is what the heap the linker script carves is bounded by.

if BOARD_IMX8MP_EVK

config KICKOS_USER_HEAP_SIZE
	int
	default 65536

endif
