# SPDX-License-Identifier: CECILL-C
# Copyright (c) 2026 Philippe Leduc

CONFIG_BOARD_FRDMK64F=y

# Only what differs from the Kconfig defaults, as savedefconfig would write it and in
# the declaration order it uses.
CONFIG_MEMORY_MODEL_MPU=y
# Not the fleet default 16: this is one Cortex-M4 core, and an application heavy enough
# to want more than eight threads on it wants a different part. The flat variant keeps 16
# because its arena starts where .bss ends. The st variant states the same figures and
# its comment carries the derivation.
CONFIG_KICKOS_MAX_THREADS=8
CONFIG_KICKOS_USER_STACK_SIZE=8192
CONFIG_KICKOS_IDLE_STACK_SIZE=512
CONFIG_KICKOS_ROOT_STACK_SIZE=8192
