CONFIG_BOARD_QEMU_RISCV=y

# The context-switch microbench. Non-enforcing, where its numbers were taken:
# a posture change moves the switch cost this variant exists to measure.
CONFIG_KICKOS_USER_STACK_SIZE=8192
CONFIG_KICKOS_IDLE_STACK_SIZE=512
CONFIG_KICKOS_ROOT_STACK_SIZE=8192
CONFIG_KICKOS_ENABLE_SELFTEST=y
CONFIG_KICKOS_BENCH=y
