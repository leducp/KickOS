# SPDX-License-Identifier: CECILL-C
# Copyright (c) 2026 Philippe Leduc
#
# Every symbol KickOS is allowed to leave WEAK. tests/static/check_seam_defaults.sh (leg 4)
# fails the build on any other weak symbol, in any archive or in the image. No entry below may
# be a backend seam: those are resolved by archive-member extraction (arch/CMakeLists.txt).
#
# One symbol per line; the RX psABI prefixes C identifiers with an underscore, so a
# KickOS-owned entry is listed in both spellings.

# --- libc interop: weak so a libc providing them wins -------------------------
# The C++ ABI's per-DSO handle.
__dso_handle
___dso_handle
# newlib's malloc re-entrancy hooks; KickOS supplies no-ops.
__malloc_lock
___malloc_lock
__malloc_unlock
___malloc_unlock

# --- hand-rolled vague linkage -----------------------------------------------
# kickos/app.h defines the per-app build stamp inline and the build force-includes that header
# into every app TU, so weak is what merges the duplicates; C has no COMDAT. The kernel's
# matching weak REFERENCE is what lets a link with no app TU at all resolve to null.
kickos_app_build_time
_kickos_app_build_time

# --- optional-symbol references (weak undefined, absence is legal) ------------
# Provided by the chip linker script; the host/sim link has no linker script, and
# kernel/domain/domain.cc + kernel/init/kmain.cc null-check every one of them.
__kickos_code_start
___kickos_code_start
__kickos_code_end
___kickos_code_end
__kickos_appdata_start
___kickos_appdata_start
__kickos_appdata_end
___kickos_appdata_end
_kickos_heap_start
__kickos_heap_start
_kickos_heap_limit
__kickos_heap_limit
# The app's own low window, carved only where a translating backend splits the image
# (docs/design-m6-mmu.md, T5b.1).
__kickos_app_rom_start
___kickos_app_rom_start
__kickos_app_rom_end
___kickos_app_rom_end
__kickos_app_sram_start
___kickos_app_sram_start
__kickos_app_sram_end
___kickos_app_sram_end
# Added to an app VIRTUAL address to name the frame the loader put those bytes in; absent is
# zero (kernel/mem/aspace.cc).
__kickos_app_load_delta
___kickos_app_load_delta
# libgcc's unwinder registration, called by the RISC-V chip bring-up only when the link
# supplied it.
__register_frame
___register_frame

# --- toolchain-owned, present only in the final image -------------------------
# Emitted by libgcc / newlib / libstdc++ or by the host glibc CRT.
__aeabi_idiv0
__aeabi_ldiv0
__aeabi_unwind_cpp_pr0
__aeabi_unwind_cpp_pr1
__aeabi_unwind_cpp_pr2
__getreent
data_start
__gmon_start__
__cxa_finalize
__cxa_finalize@GLIBC_2.2.5
_ITM_registerTMCloneTable
_ITM_deregisterTMCloneTable
