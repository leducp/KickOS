# SPDX-License-Identifier: CECILL-C
# Copyright (c) 2026 Philippe Leduc
#
# Declarations for tests/static/check_trap_redzone.sh: WHICH symbols a trap prologue can reach,
# WHICH macro holds the figure it enforces, and WHAT the graph is allowed not to know. The macro
# names are resolved through the compiler under the flags the image was built with.
#
# A CLASS is one (FRAME, DEPTH) pair with its own root set. A trap prologue reserves FRAME plus
# DEPTH at the bottom of the interrupted stack and refuses the trap when less room than that
# remains below sp. DEPTH is the half this gate MEASURES, the worst-case bytes the kernel C
# dispatch descends on that same stack; FRAME is everything else in the zone, which on armv7m
# also carries the hardware frame a preempting exception stacks and no call graph can see.
#
# File format, one record per line, '#' starts a comment:
#   arch    <arch> header=<path-relative-to-source-dir>
#   preset  <arch> <configure-preset>
#   class   <arch> <CLASS> frame=<MACRO> depth=<MACRO> [stack=trap|stack=kernel] \
#                                                        [kstacks=0|kstacks=1]
#   root    <arch> <CLASS> [?]<symbol>
#   root    <arch> <CLASS> NONE reason: <text to end of line>
#   exclude <arch> <mangled-symbol> [class=<CLASS>[,<CLASS>]...] reason: <text to end of line>
#   unsized <arch> <symbol> <bytes> [cores=1|cores>1] [calls=[?]<symbol>[,[?]<symbol>]...] \
#                                                        reason: <text to end of line>
#   recurse <arch> <symbol> <activations> reason: <text to end of line>
#   link    <arch> maps=<glob under the build directory> reason: <text to end of line>
#
# An unsized record with a scope is charged only to presets whose kernel schedules one core, or
# more than one; a symbol has at most one record per posture. Its calls= are the only edges an
# assembly body has, so a callee without '?' that the graph lacks is refused.
#
# An exclude with class= cuts the node from those classes' walks on whatever stack they spend:
# it claims the node unreachable under what those classes measure. Without class= it applies to
# every thread-stack class and to no other.
#
# A recurse record bounds a function's call to ITSELF: its frame is charged <activations> times
# and the self-edge is not walked. A record whose node has no self-edge is refused as stale.
#
# A link record narrows the corpus to the units the matching link maps LOAD: an object a LOAD
# line names, or a member of a LOADed archive, the archive a target writes beside its CMakeFiles
# directory. A glob matching no map, or a map with no LOAD line, is refused.
#
# A '?' before a root or an `exclude` marks it OPTIONAL: the symbol is compiled out under some
# option, or absent from some boards' graphs, so its absence contributes 0 instead of failing.
# A root with no '?' that is missing is a hard failure. A '?' exclusion the graph does not have
# is reported as unused.
#
# NONE declares that the class runs NO C on the guarded stack, so its depth is 0 and there is
# nothing to walk. A class with no record at all is refused: an empty root set measures 0 and
# passes forever.
#
# A `root` symbol matches the graph as an exact name or as the mangled tail of a file-scoped
# one; the match must be UNIQUE, and an ambiguous name is refused rather than guessed at.
#
# EVERY PRESET OF EVERY ARCH BELONGS HERE: a `preset` record is what makes a (board, variant)
# pair measurable at all. KICKOS_BENCH, KICKOS_TELEMETRY, KICKOS_ENABLE_SELFTEST, the memory
# model and the console backend each move a chain, and a board's variants differ by those.
#
# The top-level CMakeLists rebuilds the preset name from the board, the variant and, on an
# own-image AMP node, the node index, and asks THIS FILE whether that name is declared.
# tests/static/check_trap_redzone_decls.sh matches a record against that same rebuild, so a
# record naming anything else is reported dead.
#
# lx6 carries one trap class, PREEMPT: CHIP_ESP32 selects no HAS_MPU, so the syscall classes
# have no one to protect, while an involuntary frame still has to FIT the stack it lands on.
#
# IDLE prices idle_entry's own frames, which sit above the interrupt idle waits for.
#
# stack=trap says the descent runs on a static kernel stack the arch owns. The SPAWN FLOOR
# clause does not apply.
#
# stack=panic says the descent is the panic reporter, on the array kickos_panic_stack_enter
# moves to, compared against KICKOS_PANIC_STACK_SIZE. No other class reaches it: the walk stops
# at that entry, which is assembly and has no .ci file to carry a call edge.
#
# stack=kernel says the descent runs on the interrupted thread's PER-THREAD KERNEL block, so
# frame plus depth is compared against KICKOS_KERNEL_STACK_SIZE less its canary word, and both
# halves drop where KICKOS_KERNEL_STACKS is 0. The three stack= markers are exclusive.
#
# kstacks=<n> says the class belongs to ONE of the two entry designs an arch can compile, the
# one KICKOS_KERNEL_STACKS selects at <n>. Where the knob disagrees the image does not contain
# the path, so check_trap_redzone.sh measures the class, PRINTS that it is not enforcing it, and
# drops both halves. Only armv7m needs it; every other arch static_asserts
# KICKOS_KERNEL_STACKS != 0.

# --- rv32imac ------------------------------------------------------------------
# Two registers separate the six trap classes, both readable before the frame is
# built. mcause picks the entry: the .Lintr arms take TRAP, msip takes SWITCH, an ecall takes
# SYS or SYSPRIV, and any other exception takes FAULT. mstatus.MPP then picks the stack: an
# ecall from U-mode is SYS on the thread's kernel block, the same ecall from M-mode is SYSPRIV
# on the sp it interrupted, and every M-mode cause that is not msip or an ecall is NESTED on the
# kernel's own trap stack.
#
# EXIT measures the death path, which runs after the entry has handed control back: .Lfault
# relocates sp to the top of the dying thread's own stack and mrets into the exit stub, so the
# descent is privileged on the stack of a thread of any privilege. The SPAWN FLOOR clause is the
# only thing that bounds it.
#
# SYSPRIV is the SYS entry reached from M-mode: same FRAME_SYS, same roots, but the frame stays
# on the sp it interrupted, that thread's own stack, so it takes its own depth macro. With EXIT
# it is what sets KICKOS_MIN_STACK_SIZE on this arch, and its depth resolves per KICKOS_BENCH in
# rv_trap_stack.h.
#
# FAULT is the accepted U-mode fault: .Lfault runs the reporter with sp on the frame the entry
# built, which is the kernel block, so the whole dump chain lands there.
#
# SWITCH is the one class still on a thread stack and must not be deleted: .Lswitch and
# arch_start call kickos_arch_mpu_commit with sp = incoming->sp, which for a thread that has
# never trapped is the frame arch_context_init fabricated on its USER stack. Measuring 0 there
# is what says no privileged C frame stands on user memory.
arch    rv32imac header=arch/riscv/rv32imac/include/kickos/arch/rv_trap_stack.h
preset  rv32imac qemu-riscv
preset  rv32imac qemu-riscv-flat
preset  rv32imac qemu-riscv-bench
preset  rv32imac esp32c6-wroom
preset  rv32imac esp32c6-wroom-st
preset  rv32imac esp32c6-wroom-flat
preset  rv32imac esp32c6-wroom-bench
class   rv32imac TRAP   frame=KICKOS_RV_TRAP_FRAME depth=KICKOS_RV_TRAP_KERNEL_DEPTH stack=kernel
class   rv32imac SYS    frame=KICKOS_RV_TRAP_FRAME_SYS depth=KICKOS_RV_TRAP_KERNEL_DEPTH_SYS stack=kernel
class   rv32imac SYSPRIV frame=KICKOS_RV_TRAP_FRAME_SYS depth=KICKOS_RV_TRAP_KERNEL_DEPTH_SYSPRIV
class   rv32imac FAULT  frame=KICKOS_RV_TRAP_FRAME depth=KICKOS_RV_TRAP_NESTED_DEPTH stack=kernel
class   rv32imac SWITCH frame=KICKOS_RV_TRAP_FRAME depth=KICKOS_RV_TRAP_SWITCH_DEPTH
class   rv32imac NESTED frame=KICKOS_RV_TRAP_FRAME depth=KICKOS_RV_TRAP_NESTED_DEPTH stack=trap
class   rv32imac EXITK  frame=KICKOS_RV_TRAP_NEST_EXIT depth=KICKOS_RV_TRAP_KERNEL_DEPTH_EXITK stack=kernel
class   rv32imac RET    frame=KICKOS_RV_TRAP_NEST_EXIT depth=KICKOS_RV_TRAP_KERNEL_DEPTH_RET
class   rv32imac PANIC  frame=KICKOS_RV_PANIC_FRAME depth=KICKOS_RV_PANIC_DEPTH stack=panic
class   rv32imac IDLE   frame=KICKOS_RV_TRAP_FRAME depth=KICKOS_RV_TRAP_DEPTH_IDLE

# The symbols the .Lintr arms of switch.S branch to.
root    rv32imac TRAP kickos_isr_timer
root    rv32imac TRAP kickos_rv_dispatch_soft
root    rv32imac TRAP kickos_rv_ext_dispatch
root    rv32imac TRAP kickos_rv_ext_dispatch_dev
# Called from the prologue/epilogue itself, on the same stack, so charged to every class.
root    rv32imac TRAP kickos_arch_mpu_commit
root    rv32imac TRAP ?kickos_trace_switch_done
root    rv32imac TRAP ?kickos_bench_switch_done
# The .Lintr demux calls this ahead of the arm it picked, in selftest builds only. A sibling of
# the ISR call rather than an ancestor, so it adds to this class's MAX and not to its chain.
root    rv32imac TRAP ?kickos_rv_nested_witness

# .Lecall_slow mrets into svc_trampoline, which calls syscall_dispatch; .Lecall calls
# kickos_ipc_fastpath inline, and the taken fastpath runs the same hooks.
root    rv32imac SYS  syscall_dispatch
root    rv32imac SYS  kickos_ipc_fastpath
root    rv32imac SYS  kickos_arch_mpu_commit
root    rv32imac SYS  ?kickos_trace_switch_done
root    rv32imac SYS  ?kickos_bench_switch_done

root    rv32imac SYSPRIV syscall_dispatch
root    rv32imac SYSPRIV kickos_ipc_fastpath
root    rv32imac SYSPRIV kickos_arch_mpu_commit
root    rv32imac SYSPRIV ?kickos_trace_switch_done
root    rv32imac SYSPRIV ?kickos_bench_switch_done

# .Lfault, for a U-mode fault the entry ACCEPTED: the reporter and the stack-top query run on
# the frame the entry built, which is the kernel block. This charges the deeper of the kill and
# panic paths, so no claim about C control flow is needed.
root    rv32imac FAULT kickos_rv_fault_report
root    rv32imac FAULT kickos_fault_stack_top

# EXITK is the fault and slay stubs on the thread's own KERNEL BLOCK: .Lfault relocates sp to
# kickos_fault_stack_top, which answers with ctx.kernel_sp, and arch_ctx_redirect rebuilds the
# slay frame at that same top. RET is kickos_thread_return, which nothing relocates, so the
# SPAWN FLOOR clause is the whole of ITS enforcement. The frame term is the msip frame on both:
# the stubs reschedule, and an msip trap from M-mode keeps its frame on the interrupted sp.
root    rv32imac EXITK kickos_thread_fault_exit
root    rv32imac EXITK kickos_thread_slay_exit
root    rv32imac RET   kickos_thread_return
root    rv32imac IDLE  kmain.cc:idle_entry

# What .Lswitch itself calls, below the frame it just saved.
root    rv32imac SWITCH kickos_arch_mpu_commit
root    rv32imac SWITCH ?kickos_trace_switch_done
root    rv32imac SWITCH ?kickos_bench_switch_done

# The four .Lintr arms again, an M-mode entry running them on the trap stack instead, plus
# .Lfault's reporter. kickos_arch_mpu_commit is NOT here: .Lswitch and .Lecall are its only
# callers and neither is a nested path.
root    rv32imac NESTED kickos_isr_timer
root    rv32imac NESTED kickos_rv_dispatch_soft
root    rv32imac NESTED kickos_rv_ext_dispatch
root    rv32imac NESTED kickos_rv_ext_dispatch_dev
root    rv32imac NESTED kickos_rv_fault_report
root    rv32imac NESTED kickos_fault_stack_top
root    rv32imac NESTED ?kickos_rv_nested_witness
# .Ltrap_wild's containment call. A U-MODE entry and not a nested M-mode trap, so it is here
# for the STACK and not the cause: sp is the trap top at that label, which is the one this
# class measures. kickos_arch_mpu_commit below it is charged to SWITCH instead, the containment
# moving sp onto the incoming frame before that call exactly as .Lswitch does.
root    rv32imac NESTED kickos_rv_contain_wild_sp

# The reporter, on its own array: kickos_panic_stack_enter is switch.S, so this root is the only
# way into the console tail.
root    rv32imac PANIC kickos_panic_report

# Assembly and libgcc bodies have no .ci entry, so their frames are declared here. riscv gcc
# reports 0 bytes (static) for a frameless leaf too, so 0 is the graph's own unit and no
# return-address slot is added. Re-derive with riscv32-none-elf-objdump -d --disassemble=<sym>.
unsized rv32imac __udivdi3 0 reason: measured from the ELF, frameless register-only leaf
unsized rv32imac __umoddi3 0 reason: measured from the ELF, frameless register-only leaf
unsized rv32imac __clzsi2  0 reason: measured from the ELF, frameless register-only leaf
unsized rv32imac kickos_panic_stack_enter 0 reason: switch.S, three instructions that write no stack; jal leaves the return address in ra and the body never spills it
unsized rv32imac __divdi3  0 reason: measured from libgcc.a for this multilib, frameless register-only leaf: its body touches sp nowhere and ends in a bare ret, exactly as the two above. It is in every board's graph and only a KICKOS_BENCH measurement REACHES it, that arm's print dividing signed 64-bit where nothing else in the trap path does.

# gcc emits the complete-object destructor as a triangle node with no "bytes (static)" and an
# edge to the base-object one, and the two are the same address, so 0 is its own frame and the
# edge below it carries the real cost.
unsized rv32imac _ZN6kickos10BenchScopeD1Ev 0 reason: measured from the object, a same-address alias of the base-object destructor rather than a body of its own

# --- rxv3 ----------------------------------------------------------------------
# FOUR classes. PENDSW is the deferred-switch vector (INTB[27]); the syscall trap then splits,
# kickos_rx_syscall_trap's `cmp #56, r1` picking between a generic arm that stashes the
# [userPC][userPSW] head and RTEs into svc_trampoline, and a fastpath arm that saves the whole
# 236-byte context and runs kickos_ipc_fastpath inline. Different frame AND different descent,
# so each is its own class.
#
# The generic arm's dispatch is SYSK, stack=kernel, svc_trampoline's first act being to move R0
# onto the calling thread's own kernel block.
#
# The SYS class must not be deleted, and it is the one class here with no root at all. The head
# the trap commits before its RTE still lands on the thread's own stack, because RTE pops PC
# then PSW from R0 and R0 is the USP while PSW.U is set, so the resume USP is that head's
# address plus eight. KICKOS_RX_TRAP_NEST_SYS prices the head plus the switcher zone a SWINT can
# put below it while svc_trampoline is still on that stack.
arch    rxv3 header=arch/rx/rxv3/include/kickos/arch/rx_trap_stack.h
# Every rxv3 preset, per the knob rule above.
preset  rxv3 rx72m
preset  rxv3 rx72m-st
preset  rxv3 rx72m-flat
preset  rxv3 rx72m-bench
class   rxv3 PENDSW   frame=KICKOS_RX_TRAP_FRAME_PENDSW   depth=KICKOS_RX_TRAP_KERNEL_DEPTH_PENDSW
class   rxv3 SYS      frame=KICKOS_RX_TRAP_NEST_SYS       depth=KICKOS_RX_TRAP_KERNEL_DEPTH_SYS
class   rxv3 SYS_FAST frame=KICKOS_RX_TRAP_FRAME_SYS_FAST depth=KICKOS_RX_TRAP_KERNEL_DEPTH_SYS_FAST
class   rxv3 SYSK     frame=KICKOS_RX_TRAP_NEST_SYSK      depth=KICKOS_RX_TRAP_KERNEL_DEPTH_SYSK stack=kernel
# THE DEATH PATH. The backend rewrites the stacked PC so the RTE lands in a stub with R0 at
# kickos_fault_stack_top, which answers with ctx.kernel_sp. EXITK is the two relocating stubs on
# the block; RET is kickos_thread_return, which cannot move.
class   rxv3 EXITK    frame=KICKOS_RX_TRAP_NEST_EXIT      depth=KICKOS_RX_TRAP_KERNEL_DEPTH_EXITK stack=kernel
class   rxv3 RET      frame=KICKOS_RX_TRAP_NEST_EXIT      depth=KICKOS_RX_TRAP_KERNEL_DEPTH_RET
class   rxv3 PANIC    frame=KICKOS_RX_PANIC_FRAME         depth=KICKOS_RX_PANIC_DEPTH stack=panic

root    rxv3 PENDSW kickos_arch_mpu_commit
root    rxv3 PENDSW ?kickos_trace_switch_done
root    rxv3 PENDSW ?kickos_bench_switch_done

root    rxv3 SYS    NONE reason: svc_trampoline moves R0 to ctx.kernel_sp before it calls anything, so syscall_dispatch and the tree under it are measured as SYSK. What is left on the thread's own USP at this site writes no C frame: the trap's [userPC][userPSW] head, which the RTE's resume-USP arithmetic requires there. KICKOS_RX_TRAP_NEST_SYS prices it plus the switcher zone a SWINT can put below it. THE SECOND DOOR runs no C frame here either: .Lsvc_nokstack reaches kickos_rx_no_kernel_stack, whose console chain is far past what this class reserves, and clrpsw u puts that reporter on the ISP before the call.

root    rxv3 SYSK   syscall_dispatch
root    rxv3 SYSK   kickos_ipc_fastpath

root    rxv3 EXITK  kickos_thread_fault_exit
root    rxv3 EXITK  kickos_thread_slay_exit
root    rxv3 RET    kickos_thread_return

# The reporter, on its own array: kickos_panic_stack_enter is switch.S, so this root is the only
# way into the console tail.
root    rxv3 PANIC  kickos_panic_report

# The taken fastpath switches, so it runs the same epilogue hooks the switcher does, and after
# the transfer that epilogue is ALL it runs on a thread stack.
root    rxv3 SYS_FAST kickos_arch_mpu_commit
root    rxv3 SYS_FAST ?kickos_trace_switch_done
root    rxv3 SYS_FAST ?kickos_bench_switch_done

# On rx-elf the unit `bytes (static)` reports is the prologue push plus the incoming
# return-address slot, so a frameless leaf reports 4 and not 0: arch_bitband_present
# (`mov.l #0, r1; rts`) reports 4. arch_switch is switch.S and touches no stack at all.
unsized rxv3 arch_switch 0  reason: switch.S leaf, records the target and pends SWINT, touches no stack
unsized rxv3 kickos_panic_stack_enter 4 reason: switch.S, four instructions that write no stack of their own; the 4 is the return-address slot the caller's bsr pushed, which is the unit rx-elf reports
unsized rxv3 __udivdi3   28 reason: measured from the ELF, prologue push plus return-address slot
unsized rxv3 __umoddi3   32 reason: measured from the ELF, prologue push plus return-address slot
unsized rxv3 __divdi3    32 reason: measured from libgcc.a, pushm r6-r12 is 28 plus the return-address slot
unsized rxv3 __clzsi2     4 reason: measured from libgcc.a, frameless leaf so the return-address slot only

# --- armv7m --------------------------------------------------------------------
# THREE classes, two of them the same dispatch under the two entry designs this arch compiles.
# KICKOS_KERNEL_STACKS picks which one an image links: at 1 svc_trampoline moves SP onto the
# calling thread's own kernel block before it calls anything, so the dispatch is SVCK,
# stack=kernel; at 0 it runs the dispatch on the caller's own PSP and the dispatch is SVC, on
# the thread stack. armv7m_trap_stack.h says which chips take which.
#
# Both classes are measured on every preset and only one is enforced on any given one. The call
# graph cannot say which design an object linked; the gate reads KICKOS_KERNEL_STACKS out of the
# board config it just generated, which is what the kstacks=0 marker on SVC and stack=kernel on
# SVCK are spent on.
#
# The SVC class is what the SPAWN FLOOR clause binds. PENDSV takes FRAME_MAX, its own worst-case
# push, and is the same under both designs and therefore unmarked, handler mode putting
# everything it calls on the MSP.
arch    armv7m header=arch/arm/armv7m/include/kickos/arch/armv7m_trap_stack.h
# Every armv7m preset, per the knob rule above, grouped by board.
preset  armv7m qemu
preset  armv7m qemu-flat
preset  armv7m qemu-telem
preset  armv7m qemu-m3
preset  armv7m qemu-m3-flat
preset  armv7m qemu-m7
preset  armv7m qemu-m7-flat
preset  armv7m qemu-m33
preset  armv7m qemu-m33-flat
preset  armv7m frdmk64f
preset  armv7m frdmk64f-flat
preset  armv7m frdmk64f-st
preset  armv7m teensy41
preset  armv7m teensy41-flat
preset  armv7m teensy41-st
preset  armv7m xmc4800-relax
preset  armv7m xmc4800-relax-flat
preset  armv7m xmc4800-relax-st
preset  armv7m xmc4800-relax-bench
preset  armv7m f411disco
preset  armv7m f411disco-flat
preset  armv7m f411disco-st
preset  armv7m f411disco-bench
preset  armv7m blackpill
preset  armv7m blackpill-flat
preset  armv7m blackpill-st
preset  armv7m due
preset  armv7m due-st
preset  armv7m pizero2350
preset  armv7m pizero2350-flat
preset  armv7m pizero2350-st
preset  armv7m pizero2350-amp
preset  armv7m pizero2350-amp2-n0
preset  armv7m pizero2350-amp2-n1
# The two 64 KiB FLASH parts, whose OWN dispatch is smaller than the fleet worst case.
preset  armv7m f302nucleo
preset  armv7m f302nucleo-st
preset  armv7m bluepill-c8
preset  armv7m bluepill-c8-st
class   armv7m PENDSV frame=KICKOS_ARMV7M_TRAP_FRAME_MAX \
                      depth=KICKOS_ARMV7M_TRAP_KERNEL_DEPTH_PENDSV
# NEST_SVC_DISPATCH: the class is kstacks=0, and that posture puts the dispatch's compiler
# frames between the trampoline and the preemption point, so the preempting entry's STKALIGN pad
# stops cancelling. Provisioning it in the frame is what makes the depth comparison below
# (measured <= depth) the right test for every parity.
class   armv7m SVC    frame=KICKOS_ARMV7M_TRAP_NEST_SVC_DISPATCH \
                      depth=KICKOS_ARMV7M_TRAP_KERNEL_DEPTH_SVC kstacks=0
class   armv7m SVCK   frame=KICKOS_ARMV7M_TRAP_NEST_SVCK \
                      depth=KICKOS_ARMV7M_TRAP_KERNEL_DEPTH_SVCK stack=kernel
# The death path: no prologue guards these three classes.
#
# EXITK is the two relocating stubs wherever a block is seated. kickos_fault_stack_top answers
# with ctx.kernel_sp, so the fault redirect and the slay rebuild both land at the block TOP.
class   armv7m EXITK  frame=KICKOS_ARMV7M_TRAP_NEST_EXIT \
                      depth=KICKOS_ARMV7M_TRAP_KERNEL_DEPTH_EXITK stack=kernel
# EXIT is the fallback: f302nucleo, f302nucleo-st, due, due-st, bluepill-c8 and bluepill-c8-st
# resolve KICKOS_KERNEL_STACKS 0, so all three stubs stay on the thread's own stack against the
# spawn floor. KICKOS_KERNEL_STACKS needs the chip's HAS_MPU, which stm32f103, stm32f302 and
# sam3x8e do not select.
class   armv7m EXIT   frame=KICKOS_ARMV7M_TRAP_NEST_EXIT \
                      depth=KICKOS_ARMV7M_TRAP_KERNEL_DEPTH_EXIT kstacks=0
# RET is unmarked because it applies under BOTH designs: kickos_thread_return is an ordinary
# privileged thread's entry returning, with no fault and no redirect, so nothing relocates it.
class   armv7m RET    frame=KICKOS_ARMV7M_TRAP_NEST_EXIT \
                      depth=KICKOS_ARMV7M_TRAP_KERNEL_DEPTH_RET
class   armv7m PANIC  frame=KICKOS_ARMV7M_PANIC_FRAME \
                      depth=KICKOS_ARMV7M_PANIC_DEPTH stack=panic

root    armv7m PENDSV NONE reason: PendSV_Handler and the SVC fastpath arm run in handler mode, where ARMv7-M forces SP_main, so kickos_arch_mpu_commit, kickos_ipc_fastpath and the trace/bench hooks all descend on the MSP. What the MSP has to hold is a kernel-stack sizing question, not a privilege one, and no figure here bounds it. THE RP2350 NODE'S DOORBELL BODY IS A THIRD SUCH DESCENT AND IS UNROOTED FOR THE SAME REASON: kickos_rp2350_doorbell_service, entered from node_vectors.S, reaches kickos_amp_node_service and measures 220 bytes on pizero2350-amp2-n0 and -n1, a far call's delivery waking into the timer rearm, against the 8 KiB _kernel_stack_size rp2350.ld reserves. Rooting it under this class would compare that 220 against KICKOS_ARMV7M_TRAP_KERNEL_DEPTH_PENDSV, which is 0 by handler mode, and against the spawn floor of every armv7m preset; a class of its own would still price ONE spender of an MSP that PendSV, the SVC fastpath arm and every device ISR also spend, so it would read as a bound the tree does not have. doorbell_rp2350.cc says the same at the body.

# svc_trampoline's only call, and the SAME ROOT SET serves both classes: what differs is which
# stack the descent lands on. The fastpath is reached with lr still an EXC_RETURN, in handler
# mode, on the MSP, so it roots neither.
root    armv7m SVC    syscall_dispatch
root    armv7m SVCK   syscall_dispatch

# The two stubs that relocate, on the block.
root    armv7m EXITK  kickos_thread_fault_exit
root    armv7m EXITK  kickos_thread_slay_exit
# The same two on the thread stack, where no block is seated to move them to.
root    armv7m EXIT   kickos_thread_fault_exit
root    armv7m EXIT   kickos_thread_slay_exit
root    armv7m EXIT   kickos_thread_return
root    armv7m RET    kickos_thread_return

# The reporter, on its own array: kickos_panic_stack_enter is switch.S, so this root is the only
# way into the console tail.
root    armv7m PANIC  kickos_panic_report
unsized armv7m kickos_panic_stack_enter 0 reason: switch.S, four instructions that write no stack; bl leaves the return address in LR and the body never spills it

# The AEABI 64-bit divide helpers are libgcc, so they have no .ci entry and neither does
# __udivmoddi4 below them; measured from libgcc.a for this multilib (thumb/v7e-m+fp/softfp).
# On arm-none-eabi a frameless leaf costs 0, the return address arriving in LR rather than on
# the stack.
unsized armv7m __aeabi_ldivmod  56 reason: measured, 16-byte head plus __udivmoddi4's 40
unsized armv7m __aeabi_uldivmod 56 reason: measured, 16-byte head plus __udivmoddi4's 40

# --- armv6m --------------------------------------------------------------------
# THREE classes, the same split as armv7m: both guarded pushes in switch.S happen in HANDLER
# mode, where ARMv6-M forces SP_main (ARMv6-M ARM B1.4.1), so the switcher's calls and the SVC
# fastpath's kickos_ipc_fastpath descend on the MSP. The SVC slow path leaves handler mode and
# svc_trampoline moves SP onto the calling thread's own KERNEL BLOCK, so SVCK is what measures
# the dispatch.
#
# The SVC class must not be deleted: the fastpath arm's callee block and the trampoline's eight
# scratch bytes still land on the thread's PSP, and NEST_SVC prices them plus the exception pair
# that can preempt the window between them. PENDSV takes the push itself. There is no FP term on
# this arch.
arch    armv6m header=arch/arm/armv6m/include/kickos/arch/armv6m_trap_stack.h
# Every armv6m preset, per the knob rule above.
preset  armv6m microbit
preset  armv6m picopi
preset  armv6m picopi-st
preset  armv6m picopi-flat
class   armv6m PENDSV frame=KICKOS_ARMV6M_TRAP_FRAME \
                      depth=KICKOS_ARMV6M_TRAP_KERNEL_DEPTH_PENDSV
class   armv6m SVC    frame=KICKOS_ARMV6M_TRAP_NEST_SVC \
                      depth=KICKOS_ARMV6M_TRAP_KERNEL_DEPTH_SVC
# The death path. EXITK is the two relocating stubs on the block; RET is kickos_thread_return,
# which cannot move and stays against the spawn floor.
class   armv6m EXITK  frame=KICKOS_ARMV6M_TRAP_NEST_EXIT \
                      depth=KICKOS_ARMV6M_TRAP_KERNEL_DEPTH_EXITK stack=kernel
class   armv6m RET    frame=KICKOS_ARMV6M_TRAP_NEST_EXIT \
                      depth=KICKOS_ARMV6M_TRAP_KERNEL_DEPTH_RET
class   armv6m SVCK   frame=KICKOS_ARMV6M_TRAP_NEST_SVCK \
                      depth=KICKOS_ARMV6M_TRAP_KERNEL_DEPTH_SVCK stack=kernel
class   armv6m PANIC  frame=KICKOS_ARMV6M_PANIC_FRAME \
                      depth=KICKOS_ARMV6M_PANIC_DEPTH stack=panic

root    armv6m PENDSV NONE reason: PendSV_Handler and the SVC fastpath arm run in handler mode, where ARMv6-M forces SP_main, so kickos_arch_mpu_commit, kickos_ipc_fastpath and the telemetry hook all descend on the MSP. What the MSP has to hold is a kernel-stack sizing question, not a privilege one, and no figure here bounds it.
root    armv6m SVC    NONE reason: svc_trampoline moves SP to ctx.kernel_sp before it calls anything, so syscall_dispatch and the tree under it are measured as SVCK. What is left on the thread's PSP at this site writes no C frame at all: the fastpath arm's callee block, in handler mode, and the trampoline's own scratch push. NEST_SVC prices both plus the exception pair that can preempt the window between them.

# svc_trampoline's only call, on the calling thread's kernel block. The fastpath is reached with
# lr still an EXC_RETURN, in handler mode, on the MSP, so it roots nothing here.
root    armv6m SVCK   syscall_dispatch

root    armv6m EXITK  kickos_thread_fault_exit
root    armv6m EXITK  kickos_thread_slay_exit
root    armv6m RET    kickos_thread_return

# The reporter, on its own array: kickos_panic_stack_enter is switch.S, so this root is the only
# way into the console tail.
root    armv6m PANIC  kickos_panic_report
unsized armv6m kickos_panic_stack_enter 0 reason: switch.S, six instructions that write no stack; bl leaves the return address in LR and the body never spills it

# The AEABI helpers are libgcc, with no .ci entry and none for anything they call, so each
# figure is the symbol's own prologue plus the deepest chain below it, from libgcc.a for this
# multilib (thumb/v6-m/nofp). On arm-none-eabi a frameless leaf costs 0, the return address
# arriving in LR. The chains: __aeabi_ldivmod pushes 16 and calls __gnu_ldivmod_helper (32)
# which calls __divdi3 (40) which calls __clzdi2 (8); __aeabi_uldivmod pushes 16 and calls
# __udivmoddi4 (48) which calls __clzdi2 (8).
unsized armv6m __aeabi_ldivmod  96 reason: measured, 16-byte head plus __gnu_ldivmod_helper's 80
unsized armv6m __aeabi_uldivmod 72 reason: measured, 16-byte head plus __udivmoddi4's 56
unsized armv6m __aeabi_lmul     24 reason: measured, pushes {r4-r7,lr} then {lr}, calls nothing
unsized armv6m __aeabi_uidiv     8 reason: measured, an alias of __udivsi3 at the same address
unsized armv6m __aeabi_uidivmod  8 reason: measured, tail-branches into __udivsi3, whose worst path is its 8-byte divide-by-zero tail
unsized armv6m __clzsi2          0 reason: measured, frameless register-only leaf
unsized armv6m __ctzsi2           0 reason: measured, frameless register-only leaf

arch    lx6 header=arch/xtensa/lx6/include/kickos/arch/lx6_trap_stack.h
preset  lx6 esp32-wroom
preset  lx6 esp32-wroom-st
preset  lx6 esp32-wroom-smp
preset  lx6 esp32-wroom-bench
preset  lx6 esp32-wroom-benchsmp

# --- armv8a --------------------------------------------------------------------
# TEN classes. switch.S takes four entries: the EL1h IRQ slot, which builds its frame on the
# stack it interrupts, idle's or a privileged thread's own; the EL0 IRQ and synchronous slots,
# which build theirs on the thread's KERNEL BLOCK, SP_EL1 already naming its top; and the EL1h
# synchronous slot, whose reporter runs on the stack that faulted. Every other slot reports
# after seating SP_EL1 from the kernel's own record, or is unreachable by construction, SPSel
# never selecting SP_EL0.
#
# An EL0 entry masks interrupts and nothing in the kernel unmasks what it did not mask, so
# nothing nests on a block under IRQK or SYSK. The death-path stubs and a privileged thread's
# return run with interrupts live, so each is priced twice: EXITK and RET with an interrupt's
# whole extent nested at the deepest byte and the switch cut, EXITKSW and RETSW through the
# switch with nothing below it (armv8a_trap_stack.h).
#
# IDLE's frame term is the whole interrupt, so its zone is the idle floor.
arch    armv8a header=arch/arm64/armv8a/include/kickos/arch/armv8a_trap_stack.h
preset  armv8a qemu-arm64
preset  armv8a qemu-arm64-bench
preset  armv8a qemu-arm64-smp
preset  armv8a qemu-arm64-benchsmp
preset  armv8a qemu-arm64-benchgicv3
preset  armv8a qemu-arm64-benchsmp2
preset  armv8a qemu-arm64-smpiso
preset  armv8a qemu-arm64-gicv3
preset  armv8a qemu-arm64-amp
preset  armv8a qemu-arm64-amp2-n0
preset  armv8a qemu-arm64-amp2-n1
preset  armv8a qemu-arm64-amp3-n0
preset  armv8a qemu-arm64-amp3-n1
preset  armv8a qemu-arm64-amp3-n2
preset  armv8a imx8mp-evk
class   armv8a IRQ     frame=KICKOS_ARMV8A_TRAP_FRAME depth=KICKOS_ARMV8A_TRAP_DEPTH_IRQ
class   armv8a IRQK    frame=KICKOS_ARMV8A_TRAP_FRAME depth=KICKOS_ARMV8A_TRAP_DEPTH_IRQ \
                       stack=kernel
class   armv8a SYSK    frame=KICKOS_ARMV8A_TRAP_FRAME depth=KICKOS_ARMV8A_TRAP_DEPTH_SYSK \
                       stack=kernel
class   armv8a EXITK   frame=KICKOS_ARMV8A_TRAP_NEST depth=KICKOS_ARMV8A_TRAP_DEPTH_EXITK \
                       stack=kernel
class   armv8a EXITKSW frame=KICKOS_ARMV8A_TRAP_FRAME_NONE \
                       depth=KICKOS_ARMV8A_TRAP_DEPTH_EXITKSW stack=kernel
class   armv8a RET     frame=KICKOS_ARMV8A_TRAP_NEST depth=KICKOS_ARMV8A_TRAP_DEPTH_RET
class   armv8a RETSW   frame=KICKOS_ARMV8A_TRAP_FRAME_NONE depth=KICKOS_ARMV8A_TRAP_DEPTH_RETSW
class   armv8a FAULT   frame=KICKOS_ARMV8A_TRAP_FRAME_NONE depth=KICKOS_ARMV8A_TRAP_DEPTH_FAULT
class   armv8a IDLE    frame=KICKOS_ARMV8A_TRAP_NEST depth=KICKOS_ARMV8A_TRAP_DEPTH_IDLE
class   armv8a PANIC   frame=KICKOS_ARMV8A_PANIC_FRAME depth=KICKOS_ARMV8A_PANIC_DEPTH \
                       stack=panic

# The IRQ entry's call, and at its exit the incoming thread's unlock and bench close, which run
# below that thread's parked frame whichever stack it is on.
root    armv8a IRQ     kickos_armv8a_irq
root    armv8a IRQ     ?kickos_switch_unlock
root    armv8a IRQ     ?kickos_bench_switch_done
root    armv8a IRQK    kickos_armv8a_irq
root    armv8a IRQK    ?kickos_switch_unlock
root    armv8a IRQK    ?kickos_bench_switch_done
# The syscall, the containment call a fault makes, and the reporter the kill rule declines to.
root    armv8a SYSK    syscall_dispatch
root    armv8a SYSK    kickos_fault_kill_thread
root    armv8a SYSK    kickos_armv8a_exception
root    armv8a EXITK   kickos_thread_fault_exit
root    armv8a EXITK   kickos_thread_slay_exit
root    armv8a EXITKSW kickos_thread_fault_exit
root    armv8a EXITKSW kickos_thread_slay_exit
root    armv8a RET     kickos_thread_return
root    armv8a RETSW   kickos_thread_return
root    armv8a FAULT   kickos_armv8a_exception
root    armv8a IDLE    kmain.cc:idle_entry
root    armv8a PANIC   kickos_panic_report

exclude armv8a kickos_armv8a_switch_now class=IRQ,IRQK,EXITK,RET reason: below kickos_armv8a_irq isr_depth is nonzero for the whole dispatch, so arch_switch takes its deferred arm and never calls the swap; the IRQ exit's unlock and bench close run inside the masked swap, and a swap reached from either would be a cycle SYSK refuses. EXITK and RET price an interrupt nested at the deepest byte, and none nests below a switch frame, arch_switch running under the kernel IrqLock; EXITKSW and RETSW walk the swap instead

# switch.S bodies. From the linked qemu-arm64-smp image: kickos_armv8a_switch_now opens with
# `sub sp, sp, #0x320` and touches the stack nowhere else, its bl leaving the return address in
# x30, then moves sp onto the incoming frame and calls SWITCH_UNLOCK's kickos_switch_unlock and,
# under KICKOS_BENCH, kickos_bench_switch_done there. Those calls are its only edges.
unsized armv8a kickos_armv8a_switch_now 800 cores=1 calls=?kickos_bench_switch_done reason: SAVE_FRAME, measured from the linked image; SWITCH_UNLOCK expands to nothing at one kernel core
unsized armv8a kickos_armv8a_switch_now 800 cores>1 calls=kickos_switch_unlock,?kickos_bench_switch_done reason: SAVE_FRAME, measured from the linked image, then the unlock on the incoming frame
unsized armv8a kickos_panic_stack_enter 0 reason: switch.S, three instructions that write no stack: msr daifset, mov sp and a b into the reporter

# The page-table walks recurse once per level from LEVEL_ROOT to LEVEL_LEAF, and every outside
# call enters at LEVEL_ROOT; aspace_armv8a.cc asserts the level count.
recurse armv8a aspace_armv8a.cc:free_subtree 3 reason: levels 1 to 3, the leaf level recursing no further
recurse armv8a aspace_armv8a.cc:prune_empty 3 reason: levels 1 to 3, the leaf level returning before its loop
recurse armv8a aspace_armv8a.cc:map_into 3 reason: levels 1 to 3, the leaf level storing descriptors instead

# --- rv64imac ------------------------------------------------------------------
# TEN classes. stvec is DIRECT, so every cause enters kickos_rv64_stvec, and sstatus.SPP and
# scause pick the stack. From U-mode every cause builds its frame on the thread's KERNEL BLOCK:
# an ecall and a fault are SYSK, an interrupt IRQK, all with SIE clear to the sret. From S-mode
# an interrupt keeps its frame on the sp it interrupted, idle's or a privileged thread's own or
# a death-path stub's block, and every exception takes the per-hart trap stack, NESTED, whose
# reporter a U-mode entry that found no block seated also runs on.
#
# The death-path stubs and a privileged thread's return run with SIE set, so each is priced
# twice: EXITK and RET with an interrupt's whole extent nested at the deepest byte and the
# switch cut, EXITKSW and RETSW through the switch with nothing below it (rv64_trap_stack.h).
#
# IDLE's frame term is the whole interrupt, so its zone is idle's floor.
arch    rv64imac header=arch/riscv/rv64imac/include/kickos/arch/rv64_trap_stack.h
preset  rv64imac qemu-riscv64
preset  rv64imac qemu-riscv64-sv48
preset  rv64imac qemu-riscv64-smp
preset  rv64imac qemu-riscv64-bench
preset  rv64imac qemu-riscv64-benchsmp
preset  rv64imac qemu-riscv64-benchsmp2
class   rv64imac IRQ     frame=KICKOS_RV64_TRAP_FRAME depth=KICKOS_RV64_TRAP_DEPTH_IRQ
class   rv64imac IRQK    frame=KICKOS_RV64_TRAP_FRAME depth=KICKOS_RV64_TRAP_DEPTH_IRQ \
                         stack=kernel
class   rv64imac SYSK    frame=KICKOS_RV64_TRAP_FRAME depth=KICKOS_RV64_TRAP_DEPTH_SYSK \
                         stack=kernel
class   rv64imac NESTED  frame=KICKOS_RV64_TRAP_FRAME depth=KICKOS_RV64_TRAP_NESTED_DEPTH \
                         stack=trap
class   rv64imac EXITK   frame=KICKOS_RV64_TRAP_NEST depth=KICKOS_RV64_TRAP_DEPTH_EXITK \
                         stack=kernel
class   rv64imac EXITKSW frame=KICKOS_RV64_TRAP_FRAME_NONE \
                         depth=KICKOS_RV64_TRAP_DEPTH_EXITKSW stack=kernel
class   rv64imac RET     frame=KICKOS_RV64_TRAP_NEST depth=KICKOS_RV64_TRAP_DEPTH_RET
class   rv64imac RETSW   frame=KICKOS_RV64_TRAP_FRAME_NONE depth=KICKOS_RV64_TRAP_DEPTH_RETSW
class   rv64imac IDLE    frame=KICKOS_RV64_TRAP_NEST depth=KICKOS_RV64_TRAP_DEPTH_IDLE
class   rv64imac PANIC   frame=KICKOS_RV64_PANIC_FRAME depth=KICKOS_RV64_PANIC_DEPTH \
                         stack=panic

# .Lintr's call, and at its exit a booked switch's unlock, which runs below the incoming
# thread's parked frame whichever stack it is on.
root    rv64imac IRQ     kickos_rv64_isr_dispatch
root    rv64imac IRQ     ?kickos_switch_unlock
root    rv64imac IRQK    kickos_rv64_isr_dispatch
root    rv64imac IRQK    ?kickos_switch_unlock
# .Lecall's dispatch, and .Lfault's reporter with the containment call inside it.
root    rv64imac SYSK    syscall_dispatch
root    rv64imac SYSK    kickos_rv64_fault_report
root    rv64imac NESTED  kickos_rv64_fault_report
root    rv64imac NESTED  kickos_rv64_no_kernel_stack
root    rv64imac EXITK   kickos_thread_fault_exit
root    rv64imac EXITK   kickos_thread_slay_exit
root    rv64imac EXITKSW kickos_thread_fault_exit
root    rv64imac EXITKSW kickos_thread_slay_exit
root    rv64imac RET     kickos_thread_return
root    rv64imac RETSW   kickos_thread_return
root    rv64imac IDLE    kmain.cc:idle_entry
root    rv64imac PANIC   kickos_panic_report

exclude rv64imac kickos_rv64_switch_now class=IRQ,IRQK,EXITK,RET reason: below .Lintr isr_depth is nonzero for the whole dispatch, so arch_switch books the target and never calls the swap. EXITK and RET price an interrupt nested at the deepest byte, and none nests below a switch frame, arch_switch running under the kernel IrqLock; EXITKSW and RETSW walk the swap instead

# switch.S bodies. kickos_rv64_switch_now opens with `addi sp, sp, -FRAME` and touches the stack
# nowhere else, its caller's ra in the frame, then moves sp onto the incoming frame and calls
# BENCH_SW_CLOSE's kickos_bench_switch_done and SWITCH_UNLOCK's kickos_switch_unlock there.
unsized rv64imac kickos_rv64_switch_now 256 cores=1 calls=?kickos_bench_switch_done reason: KICKOS_RV64_FRAME; SWITCH_UNLOCK expands to nothing at one kernel core
unsized rv64imac kickos_rv64_switch_now 256 cores>1 calls=kickos_switch_unlock,?kickos_bench_switch_done reason: KICKOS_RV64_FRAME, then the unlock on the incoming frame
unsized rv64imac kickos_panic_stack_enter 0 reason: switch.S, a csrci, a mv and a tail into the reporter, none of which writes the stack
# cmake/kernel_runtime_rv64imac.syms rewrites the kernel archives' calls to these two libgcc
# names onto libgcc_rv64imac.cc's bodies, so the edge is the rename and the frame is theirs.
unsized rv64imac __clzdi2 0 calls=kickos_rv64_clzdi2 reason: renamed at link to kickos_rv64_clzdi2, which the graph sizes
unsized rv64imac __ctzdi2 0 calls=kickos_rv64_ctzdi2 reason: renamed at link to kickos_rv64_ctzdi2, which the graph sizes

# The page-table walks recurse once per level from LEVEL_ROOT to LEVEL_LEAF, and every outside
# call enters at LEVEL_ROOT; aspace_rv64imac.cc asserts sv48's four levels are the most.
recurse rv64imac aspace_rv64imac.cc:free_subtree 4 reason: one activation per level, the leaf level recursing no further
recurse rv64imac aspace_rv64imac.cc:prune_empty 4 reason: one activation per level, the leaf level returning before its loop
recurse rv64imac aspace_rv64imac.cc:map_into 4 reason: one activation per level, the leaf level storing descriptors instead

# --- x86_64 --------------------------------------------------------------------
# TWELVE classes. A ring 3 entry builds its frame on the thread's KERNEL BLOCK: an interrupt
# or an exception through the task-state segment's rsp0 is IRQK, SYSCALL through the per-core
# block is SYSK, both with the interrupt flag clear to the return. A ring 0 interrupt keeps its
# frame on the rsp it interrupted, IRQ, idle's or a privileged thread's own or a death-path
# stub's block, and the double fault, NMI and machine check take static interrupt-stack-table
# arrays, IST. A privileged caller's arch_syscall jumps to the dispatch on its own stack with
# interrupts live, SYSPRIV.
#
# The death-path stubs, a privileged thread's return and a privileged syscall run with
# interrupts live, so each is priced twice: EXITK, RET and SYSPRIV with an interrupt's whole
# extent nested at the deepest byte and the switch cut, EXITKSW, RETSW and SYSPRIVSW through
# the switch with nothing below it (x86_64_trap_stack.h).
#
# The build also compiles the kernel-free boot images' objects, whose declining stubs define
# the kernel's own entry points, so the corpus is what the application images' maps LOAD.
arch    x86_64 header=arch/x86/x86_64/include/kickos/arch/x86_64_trap_stack.h
preset  x86_64 qemu-x86_64
preset  x86_64 qemu-x86_64-bench
link    x86_64 maps=**/*.efi.map reason: kickos_x86_64_link_image writes one per application image (cmake/x86_64_image.cmake), and the boot images it does not link write none
class   x86_64 IRQ       frame=KICKOS_X86_64_TRAP_FRAME_IRQ depth=KICKOS_X86_64_TRAP_DEPTH_IRQ
class   x86_64 IRQK      frame=KICKOS_X86_64_TRAP_FRAME depth=KICKOS_X86_64_TRAP_DEPTH_IRQ \
                         stack=kernel
class   x86_64 SYSK      frame=KICKOS_X86_64_TRAP_FRAME depth=KICKOS_X86_64_TRAP_DEPTH_SYSK \
                         stack=kernel
class   x86_64 SYSPRIV   frame=KICKOS_X86_64_TRAP_NEST depth=KICKOS_X86_64_TRAP_DEPTH_SYSPRIV
class   x86_64 SYSPRIVSW frame=KICKOS_X86_64_TRAP_FRAME_NONE \
                         depth=KICKOS_X86_64_TRAP_DEPTH_SYSPRIVSW
class   x86_64 IST       frame=KICKOS_X86_64_TRAP_FRAME depth=KICKOS_X86_64_TRAP_DEPTH_IST \
                         stack=trap
class   x86_64 EXITK     frame=KICKOS_X86_64_TRAP_NEST depth=KICKOS_X86_64_TRAP_DEPTH_EXITK \
                         stack=kernel
class   x86_64 EXITKSW   frame=KICKOS_X86_64_TRAP_FRAME_NONE \
                         depth=KICKOS_X86_64_TRAP_DEPTH_EXITKSW stack=kernel
class   x86_64 RET       frame=KICKOS_X86_64_TRAP_NEST depth=KICKOS_X86_64_TRAP_DEPTH_RET
class   x86_64 RETSW     frame=KICKOS_X86_64_TRAP_FRAME_NONE depth=KICKOS_X86_64_TRAP_DEPTH_RETSW
class   x86_64 IDLE      frame=KICKOS_X86_64_TRAP_NEST depth=KICKOS_X86_64_TRAP_DEPTH_IDLE
class   x86_64 PANIC     frame=KICKOS_X86_64_PANIC_FRAME depth=KICKOS_X86_64_PANIC_DEPTH \
                         stack=panic

# kickos_x86_64_trap_common's call: the vector's service, the containment call and the report
# are all under it. A switch the service books resumes a frame and runs no C on the incoming
# stack.
root    x86_64 IRQ       kickos_x86_64_trap
root    x86_64 IRQK      kickos_x86_64_trap
root    x86_64 IST       kickos_x86_64_trap
root    x86_64 SYSK      syscall_dispatch
root    x86_64 SYSPRIV   syscall_dispatch
root    x86_64 SYSPRIVSW syscall_dispatch
root    x86_64 EXITK     kickos_thread_fault_exit
root    x86_64 EXITK     kickos_thread_slay_exit
root    x86_64 EXITKSW   kickos_thread_fault_exit
root    x86_64 EXITKSW   kickos_thread_slay_exit
root    x86_64 RET       kickos_thread_return
root    x86_64 RETSW     kickos_thread_return
root    x86_64 IDLE      kmain.cc:idle_entry
root    x86_64 PANIC     kickos_panic_report

exclude x86_64 kickos_x86_64_switch_now class=IRQ,IRQK,IST,SYSPRIV,EXITK,RET reason: below kickos_x86_64_isr g_isr_depth is nonzero for the whole service, so arch_switch books the target and never calls the swap, and neither the containment call nor the report switches. SYSPRIV, EXITK and RET price an interrupt nested at the deepest byte, and none nests below a switch frame, arch_switch running under the kernel IrqLock; SYSPRIVSW, EXITKSW and RETSW walk the swap instead

# switch.S bodies, each charged its own return-address slot as gcc charges a C body.
# kickos_x86_64_switch_now is the call's 8, `pushfq` and `subq $KOS_FRAME_SIZE`, then it moves
# rsp onto the incoming frame and, under KICKOS_BENCH, calls kickos_bench_switch_done there.
unsized x86_64 kickos_x86_64_switch_now 192 calls=?kickos_bench_switch_done reason: 8 + 8 + 176 read off switch.S; the incoming frame's resume is a jmp and runs no C
unsized x86_64 kickos_panic_stack_enter 8 reason: panic_stack.S, the call's return address on the stack it leaves, then a cli, a mov and a jmp; the pushq $0 lands on the reporter's array and is KICKOS_X86_64_PANIC_FRAME

# --- the corpus floor ----------------------------------------------------------
# SECONDARY, and exactly one record per arch. Which units a run measures is settled against
# the build's own compile database; what is left for a count to catch is a tree whose compile
# database is ITSELF near empty. So these are gross guards, and none sits within a few units of
# a board that legitimately builds small. Per arch, the corpus varying more across arches than
# within one.
#
# EVERY RECORD NAMES ITS SCOPE: a minimum is only a minimum over the presets actually swept.
floor   armv7m   files=85 nodes=600 reason: all 38 declared armv7m presets swept 2026-09-12; smallest 107 files on teensy41-flat and 815 nodes on bluepill-c8 and due, largest 158 files on xmc4800-relax-bench and 1067 nodes on pizero2350-amp2-n0
floor   armv6m   files=85 nodes=600 reason: all 4 declared armv6m presets swept 2026-09-12; smallest 109 files/836 nodes on picopi-flat, largest 138 files on microbit and 974 nodes on picopi-st
floor   rv32imac files=75 nodes=700 reason: all 7 declared rv32imac presets swept 2026-09-12; smallest 99 files/830 nodes on esp32c6-wroom-flat, largest 134 files on esp32c6-wroom-st and 971 nodes on qemu-riscv-bench
floor   rxv3     files=75 nodes=650 reason: all 4 declared rxv3 presets swept, the first three 2026-09-12 and rx72m-bench 2026-09-20; smallest 99 files/852 nodes on rx72m-flat, largest 127 files/1101 nodes on rx72m-bench
floor   armv8a   files=90 nodes=700 reason: all 15 declared armv8a presets swept 2026-09-24; smallest 134 files/1173 nodes on qemu-arm64, largest 140 files on qemu-arm64-benchsmp, -benchgicv3 and -benchsmp2 and 1362 nodes on qemu-arm64-benchgicv3
floor   rv64imac files=90 nodes=700 reason: all 6 declared rv64imac presets swept 2026-09-25; smallest 133 files/1206 nodes on qemu-riscv64 and -sv48, largest 137 files on -benchsmp and -benchsmp2 and 1372 nodes on -benchsmp
floor   x86_64   files=85 nodes=700 reason: both declared x86_64 presets swept 2026-09-25, counted after the units no application map LOADs; 108 files/1022 nodes on qemu-x86_64, 110 files/1063 nodes on qemu-x86_64-bench
floor   lx6      files=75 nodes=650 reason: all 5 declared lx6 presets swept, the first three 2026-09-12, esp32-wroom-bench 2026-09-13 and esp32-wroom-benchsmp 2026-09-22; smallest 103 files/909 nodes on esp32-wroom, largest 127 files/1182 nodes on esp32-wroom-benchsmp
class   lx6 PREEMPT frame=KICKOS_LX6_TRAP_FRAME depth=KICKOS_LX6_TRAP_DEPTH
class   lx6 PANIC   frame=KICKOS_LX6_PANIC_FRAME depth=KICKOS_LX6_PANIC_DEPTH stack=panic
class   lx6 IDLE    frame=KICKOS_LX6_TRAP_FRAME depth=KICKOS_LX6_TRAP_DEPTH_IDLE
root    lx6 PREEMPT kickos_lx6_dispatch_l1
root    lx6 IDLE    kmain.cc:idle_entry

# The reporter, on its own array: kickos_panic_stack_enter is switch.S, so this root is the only
# way into the console tail.
root    lx6 PANIC   kickos_panic_report

# Three nodes with no .ci entry. Measured from the xtensa-esp-elf 16.1.0 esp32 multilib: the
# windowed ABI states a frame in the `entry` immediate, so each figure is that plus the deepest
# chain below it. __divdi3 is referenced but linked into no image, so it is read from libgcc.a.
unsized lx6 kickos_panic_stack_enter 32 reason: switch.S. The windowed ABI gives it no way to write the incoming a1 without opening a frame first, so unlike every other backend's entry this one spends `entry a1, 32` on the stack it is leaving
unsized lx6 __divdi3      64 reason: entry a1 32, then a call0 into __udivdi3 which entries another 32
unsized lx6 __udivdi3     32 reason: entry a1 32, calls nothing
unsized lx6 xtensa_switch 128 cores=1 reason: at one kernel core SWITCH_UNLOCK (arch/xtensa/lx6/include/kickos/arch/lx6_switch_unlock.inc) expands to nothing and the swap calls nothing, its own entry a1 being 32 in the linked esp32-wroom image. 128 is the swap-unlock descent this record carried before the rings, kept so the one-core figure does not move, and 96 of it is reserved unused, which is the safe direction
unsized lx6 xtensa_switch 32 cores>1 calls=kickos_switch_unlock reason: entry a1 32 in the linked esp32-wroom-smp image, and the swap ends in SWITCH_UNLOCK (arch/xtensa/lx6/include/kickos/arch/lx6_switch_unlock.inc), a call4 into kickos_switch_unlock, whose descent the graph carries
