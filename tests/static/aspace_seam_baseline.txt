# SPDX-License-Identifier: CECILL-C
# Copyright (c) 2026 Philippe Leduc
#
# The commit the address-space seam was frozen at, which is what
# tests/static/check_aspace_sigdiff.sh diffs against by default. docs/design-m6-mmu.md
# section 3.4b names what T2 froze.
#
# THE DIFF IS THE DELIVERABLE AND IT IS NOT EMPTY. F8 asked whether a second backend moves
# these signatures; measured at M6.3's R2.1, one member was owed and it landed at R2.2, so the
# instrument REFUSES by design and its refusal is the milestone's result. A reader meeting that
# refusal must not move this ref to quiet it: that deletes the finding rather than fixing
# anything. R5 records the verdict and what it says beyond the diff.
#
# One bare ref, comments and blank lines ignored. A full hash rather than a branch name: a
# branch moves, and a baseline that moves with the tree can never report a difference.
f0360d3ae88a4958358ecd8a2769119bff455265
